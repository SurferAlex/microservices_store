name: CI Pipeline

# Когда запускается workflow
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

# Задачи (jobs)
jobs:
  # Задача: проверка кода
  check:
    name: Проверка кода
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Получение кода
        uses: actions/checkout@v4
      
      - name: 🔧 Установка Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      
      - name: 🔍 Проверка форматирования auth_service
        working-directory: ./auth_service
        run: |
          echo "📝 Проверяю форматирование кода auth_service..."
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "❌ Код не отформатирован! Запустите: gofmt -s -w ."
            gofmt -s -d .
            exit 1
          fi
          echo "✅ Форматирование корректно!"
      
      - name: 🔍 Проверка форматирования profile_service
        working-directory: ./profile_service
        run: |
          echo "📝 Проверяю форматирование кода profile_service..."
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "❌ Код не отформатирован! Запустите: gofmt -s -w ."
            gofmt -s -d .
            exit 1
          fi
          echo "✅ Форматирование корректно!"
      
      - name: 🔍 Статический анализ auth_service
        working-directory: ./auth_service
        run: |
          echo "🔎 Запускаю go vet для auth_service..."
          go vet ./...
          echo "✅ Статический анализ пройден!"
      
      - name: 🔍 Статический анализ profile_service
        working-directory: ./profile_service
        run: |
          echo "🔎 Запускаю go vet для profile_service..."
          go vet ./...
          echo "✅ Статический анализ пройден!"
      
      - name: 📦 Проверка зависимостей auth_service
        working-directory: ./auth_service
        run: |
          echo "📋 Проверяю go.mod и go.sum для auth_service..."
          go mod tidy
          if [ -n "$(git status --porcelain)" ]; then
            echo "❌ go.mod или go.sum требуют обновления! Запустите: go mod tidy"
            git status
            exit 1
          fi
          echo "✅ Зависимости актуальны!"
      
      - name: 📦 Проверка зависимостей profile_service
        working-directory: ./profile_service
        run: |
          echo "📋 Проверяю go.mod и go.sum для profile_service..."
          go mod tidy
          if [ -n "$(git status --porcelain)" ]; then
            echo "❌ go.mod или go.sum требуют обновления! Запустите: go mod tidy"
            git status
            exit 1
          fi
          echo "✅ Зависимости актуальны!"
      
      - name: 📦 Проверка компиляции auth_service
        working-directory: ./auth_service
        run: |
          echo "🔍 Проверяю auth_service..."
          go mod download
          go build ./cmd/main.go
          echo "✅ auth_service компилируется успешно!"
      
      - name: 📦 Проверка компиляции profile_service
        working-directory: ./profile_service
        run: |
          echo "🔍 Проверяю profile_service..."
          go mod download
          go build ./cmd/main.go
          echo "✅ profile_service компилируется успешно!"
      
      - name: ✅ Все проверки пройдены
        run: |
          echo "🎉 Все проверки качества кода пройдены успешно!"